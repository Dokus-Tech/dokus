# Cloud/Production Configuration
# Extends application.conf with cloud-specific overrides

include "application.conf"

ktor {
    deployment {
        environment = "cloud"
        environment = ${?ENVIRONMENT}
    }
}

caching {
    type = "redis"
    type = ${?CACHE_TYPE}
}
