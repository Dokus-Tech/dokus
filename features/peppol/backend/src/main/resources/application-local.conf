# Local Development Configuration
# Extends application.conf with local-specific overrides

include "application.conf"

# Override with minimal local settings
# Most settings come from base application.conf
