package ai.dokus.auth.backend.config

import ai.dokus.foundation.ktor.AppBaseConfig
import io.ktor.server.application.Application

fun Application.configureAuthentication(appConfig: AppBaseConfig) {
}