apiVersion: apps/v1
kind: Deployment
metadata:
  name: dokus-auth-service
  namespace: dokus
  labels:
    app: dokus-auth
    component: backend
    tier: service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dokus-auth
  template:
    metadata:
      labels:
        app: dokus-auth
        version: v1
    spec:
      serviceAccountName: dokus-auth
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
      - name: auth-service
        image: registry.dokus.ai/dokus/auth-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        - containerPort: 9090
          name: metrics
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: DB_URL
          valueFrom:
            secretKeyRef:
              name: dokus-auth-db
              key: url
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: dokus-auth-db
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dokus-auth-db
              key: password
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: dokus-auth-jwt
              key: secret
        - name: JWT_ISSUER
          value: "https://api.dokus.ai"
        - name: JWT_AUDIENCE
          value: "dokus-api"
        - name: REDIS_HOST
          value: "dokus-redis-service"
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dokus-redis
              key: password
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: logs
          mountPath: /var/log/dokus
      volumes:
      - name: config
        configMap:
          name: dokus-auth-config
      - name: logs
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: dokus-auth-service
  namespace: dokus
  labels:
    app: dokus-auth
spec:
  type: ClusterIP
  selector:
    app: dokus-auth
  ports:
  - port: 8081
    targetPort: http
    protocol: TCP
    name: http
  - port: 9090
    targetPort: metrics
    protocol: TCP
    name: metrics
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dokus-auth
  namespace: dokus
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dokus-auth-config
  namespace: dokus
data:
  application.conf: |
    ktor {
        deployment {
            port = 8081
            host = "0.0.0.0"
        }
    }

    security {
        cors {
            allowedHosts = ["dokus.ai", "10.13.4.103"]
        }
    }
---
apiVersion: v1
kind: Secret
metadata:
  name: dokus-auth-db
  namespace: dokus
type: Opaque
stringData:
  url: "jdbc:postgresql://postgres-service:5432/dokus_auth"
  username: "dokus_user"
  password: "change-in-production"
---
apiVersion: v1
kind: Secret
metadata:
  name: dokus-auth-jwt
  namespace: dokus
type: Opaque
stringData:
  secret: "change-this-secret-in-production"
---
apiVersion: v1
kind: Secret
metadata:
  name: dokus-redis
  namespace: dokus
type: Opaque
stringData:
  password: "redis-password-change-in-production"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dokus-auth-hpa
  namespace: dokus
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dokus-auth-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dokus-auth-pdb
  namespace: dokus
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: dokus-auth
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dokus-auth-netpol
  namespace: dokus
spec:
  podSelector:
    matchLabels:
      app: dokus-auth
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: dokus
    - podSelector:
        matchLabels:
          app: dokus-gateway
    ports:
    - protocol: TCP
      port: 8081
  - from:
    - namespaceSelector:
        matchLabels:
          name: monitoring
    ports:
    - protocol: TCP
      port: 9090
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: dokus
    - podSelector:
        matchLabels:
          app: postgres
    ports:
    - protocol: TCP
      port: 5432
  - to:
    - namespaceSelector:
        matchLabels:
          name: dokus
    - podSelector:
        matchLabels:
          app: redis
    ports:
    - protocol: TCP
      port: 6379
  - ports:
    - protocol: UDP
      port: 53