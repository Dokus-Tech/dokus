name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

# Cancel in-progress runs for the same workflow and ref
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: write
  checks: write
  pull-requests: write
  actions: read

env:
  GRADLE_OPTS: -Dorg.gradle.daemon=false -Dorg.gradle.parallel=true -Dorg.gradle.caching=true
  JAVA_VERSION: '21'

jobs:
  # Quick validation checks that fail fast
  validation:
    name: 🔍 Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ✅ Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: 📊 Check for TODO/FIXME
        run: |
          echo "Checking for unresolved TODOs and FIXMEs..."
          if git diff origin/${{ github.base_ref }}...HEAD | grep -E "^\+.*\b(TODO|FIXME)\b" | grep -v "github/workflows"; then
            echo "⚠️ Warning: New TODOs or FIXMEs found in this PR"
          fi
        continue-on-error: true

  # Determine which platforms need testing
  changes:
    name: 🔎 Detect Changes
    runs-on: ubuntu-latest
    outputs:
      android: ${{ steps.filter.outputs.android }}
      ios: ${{ steps.filter.outputs.ios }}
      desktop: ${{ steps.filter.outputs.desktop }}
      wasm: ${{ steps.filter.outputs.wasm }}
      common: ${{ steps.filter.outputs.common }}
      has-changes: ${{ steps.filter.outputs.changes }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Check changed files
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            android:
              - 'composeApp/src/androidMain/**'
              - 'features/**/androidMain/**'
              - 'foundation/**/androidMain/**'
            ios:
              - 'composeApp/src/iosMain/**'
              - 'features/**/iosMain/**'
              - 'foundation/**/iosMain/**'
            desktop:
              - 'composeApp/src/desktopMain/**'
              - 'composeApp/src/jvmMain/**'
              - 'features/**/jvmMain/**'
              - 'foundation/**/jvmMain/**'
            wasm:
              - 'composeApp/src/wasmJsMain/**'
              - 'features/**/wasmJsMain/**'
              - 'foundation/**/wasmJsMain/**'
            common:
              - 'composeApp/src/commonMain/**'
              - 'features/**/commonMain/**'
              - 'foundation/**/commonMain/**'
              - '*.gradle.kts'
              - 'gradle/**'

  # Code quality checks
  lint:
    name: 🧹 Code Quality
    needs: validation
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔧 Setup environment
        uses: ./.github/actions/job-set-up

      - name: 🧹 Run ktlint
        run: |
          echo "Running Kotlin linter..."
          ./gradlew ktlintCheck --continue || echo "⚠️ Linting issues found"
        continue-on-error: true

      - name: 📊 Generate dependency graph
        run: ./gradlew dependencies > dependencies.txt || true

      - name: 📤 Upload dependency report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dependency-report
          path: dependencies.txt
          retention-days: 7

  # Run tests for affected platforms
  test:
    name: 🧪 Tests
    needs: [validation, changes]
    if: |
      github.event_name == 'push' ||
      needs.changes.outputs.android == 'true' ||
      needs.changes.outputs.ios == 'true' ||
      needs.changes.outputs.desktop == 'true' ||
      needs.changes.outputs.common == 'true'
    uses: ./.github/workflows/test.yml
    with:
      # Common changes affect ALL platforms
      run-android: ${{ github.event_name == 'push' || needs.changes.outputs.common == 'true' || needs.changes.outputs.android == 'true' }}
      run-desktop: ${{ github.event_name == 'push' || needs.changes.outputs.common == 'true' || needs.changes.outputs.desktop == 'true' }}
      run-ios: ${{ needs.changes.outputs.common == 'true' || needs.changes.outputs.ios == 'true' }}  # Enable iOS tests when common changes

  # Build Android APK
  build-android:
    name: 🏗️ Build Android
    needs: [test, changes]
    if: |
      always() &&
      (needs.test.result == 'success' || needs.test.result == 'skipped') &&
      (github.ref == 'refs/heads/main' || github.event_name == 'push' || needs.changes.outputs.common == 'true' || needs.changes.outputs.android == 'true')
    uses: ./.github/workflows/build-android.yml
    with:
      build-type: ${{ github.ref == 'refs/heads/main' && 'release' || 'debug' }}
      create-release: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    secrets: inherit

  # Build macOS DMG
  build-macos:
    name: 🏗️ Build macOS
    needs: [test, changes]
    if: |
      always() &&
      (needs.test.result == 'success' || needs.test.result == 'skipped') &&
      (github.ref == 'refs/heads/main' || github.event_name == 'push' || needs.changes.outputs.common == 'true' || needs.changes.outputs.desktop == 'true')
    uses: ./.github/workflows/build-macos.yml
    with:
      build-type: ${{ github.ref == 'refs/heads/main' && 'release' || 'debug' }}
      create-release: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    secrets: inherit

  release-apple:
    name: 🚀 Release Apple (iOS & macOS)
    needs: [test]
    if: |
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      (needs.test.result == 'success' || needs.test.result == 'skipped')
    uses: ./.github/workflows/release-apple.yml
    with:
      run-ios: true
      run-macos: true
      include-dmg: false
    secrets: inherit

  # Create deployment package for releases
  create-deployment-package:
    name: 📦 Create Deployment Package
    needs: [ test ]
    if: |
      always() &&
      (needs.test.result == 'success' || needs.test.result == 'skipped') &&
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Create deployment package
        run: |
          # Create deployment archive
          cd deployment
          zip -r ../dokus-quick-deploy.zip . -x "*.git*" -x ".env" -x ".env.backup"
          cd ..

          # Show contents
          echo "📦 Package contents:"
          unzip -l dokus-quick-deploy.zip

          # Get file size
          SIZE=$(du -h dokus-quick-deploy.zip | cut -f1)
          echo "📊 Package size: $SIZE"

      - name: 📤 Upload deployment package
        uses: actions/upload-artifact@v4
        with:
          name: deployment-package
          path: dokus-quick-deploy.zip
          retention-days: 90

  # Summary and status reporting
  ci-status:
    name: 📊 CI Status
    runs-on: ubuntu-latest
    needs: [ validation, changes, lint, test, build-android, build-macos, create-deployment-package ]
    if: always()

    steps:
      - name: 📊 Generate status report
        run: |
          echo "# 📊 CI Pipeline Status Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Job Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Validation | ${{ needs.validation.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Code Quality | ${{ needs.lint.result == 'success' && '✅ Passed' || needs.lint.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Tests | ${{ needs.test.result == 'success' && '✅ Passed' || needs.test.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build Android | ${{ needs.build-android.result == 'success' && '✅ Passed' || needs.build-android.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build macOS | ${{ needs.build-macos.result == 'success' && '✅ Passed' || needs.build-macos.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Deployment Package | ${{ needs.create-deployment-package.result == 'success' && '✅ Passed' || needs.create-deployment-package.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Platform changes detected
          echo "## 🔍 Changes Detected" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Android: ${{ needs.changes.outputs.android == 'true' && '✅ Yes' || '➖ No' }}" >> $GITHUB_STEP_SUMMARY
          echo "- iOS: ${{ needs.changes.outputs.ios == 'true' && '✅ Yes' || '➖ No' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Desktop: ${{ needs.changes.outputs.desktop == 'true' && '✅ Yes' || '➖ No' }}" >> $GITHUB_STEP_SUMMARY
          echo "- WASM: ${{ needs.changes.outputs.wasm == 'true' && '✅ Yes' || '➖ No' }}" >> $GITHUB_STEP_SUMMARY

      - name: ✅ Check overall status
        run: |
          if [ "${{ needs.validation.result }}" == "failure" ] || \
             [ "${{ needs.test.result }}" == "failure" ] || \
             [ "${{ needs.build-android.result }}" == "failure" ] || \
             [ "${{ needs.build-macos.result }}" == "failure" ] || \
             [ "${{ needs.create-deployment-package.result }}" == "failure" ]; then
            echo "❌ CI Pipeline Failed"
            exit 1
          fi
          echo "✅ CI Pipeline Passed"

  # Automatic release on main branch
  release:
    name: 🚀 Create Release
    needs: [ build-android, build-macos, create-deployment-package ]
    if: |
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      needs.build-android.result == 'success' &&
      needs.build-macos.result == 'success' &&
      needs.create-deployment-package.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷️ Generate version tag
        id: version
        run: |
          # Generate version from commit count and short SHA
          VERSION_CODE=$(git rev-list --count HEAD)
          SHORT_SHA=$(git rev-parse --short HEAD)
          VERSION_TAG="v0.0.${VERSION_CODE}"

          echo "version-tag=$VERSION_TAG" >> $GITHUB_OUTPUT
          echo "version-code=$VERSION_CODE" >> $GITHUB_OUTPUT
          echo "short-sha=$SHORT_SHA" >> $GITHUB_OUTPUT

          echo "📦 Version: $VERSION_TAG (build $VERSION_CODE)"

      - name: 📝 Generate changelog
        id: changelog
        run: |
          # Get commits since last tag
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")

          if [ -z "$LAST_TAG" ]; then
            COMMITS=$(git log --pretty=format:"- %s (%h)" -n 10)
          else
            COMMITS=$(git log ${LAST_TAG}..HEAD --pretty=format:"- %s (%h)")
          fi

          # Save to file for multiline
          echo "$COMMITS" > changelog.txt

          echo "📋 Generated changelog"

      - name: 📥 Download Android APK
        uses: actions/download-artifact@v4
        with:
          name: release-apk
          path: ./artifacts

      - name: 📥 Download macOS DMG
        uses: actions/download-artifact@v4
        with:
          name: release-dmg
          path: ./artifacts

      - name: 📥 Download Backend Deployment Package
        uses: actions/download-artifact@v4
        with:
          name: deployment-package
          path: ./artifacts

      - name: 🚀 Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version-tag }}
          name: Release ${{ steps.version.outputs.version-tag }}
          body: |
            # 🚀 Dokus Release ${{ steps.version.outputs.version-tag }}

            **Build:** ${{ steps.version.outputs.version-code }}
            **Commit:** ${{ steps.version.outputs.short-sha }}
            **Branch:** ${{ github.ref_name }}
            **Date:** ${{ github.event.head_commit.timestamp }}

            ## 📝 Changes

            ${{ steps.changelog.outputs.commits }}

            ## 📦 Downloads

            ### 🖥️ Desktop & Mobile Apps
            - **Android APK**: See assets below
            - **macOS DMG**: See assets below
            - **Release Bundle (AAB)**: Available in [workflow artifacts](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})

            ### 🐳 Backend Server
            - **Quick Deploy Package**: `dokus-quick-deploy.zip` - One-click deployment for any platform

            ## 🚀 Quick Start

            ### Deploy Backend Server

            **Linux / macOS:**
            ```bash
            curl -L https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version-tag }}/dokus-quick-deploy.zip -o dokus.zip
            unzip dokus.zip -d dokus && cd dokus
            chmod +x dokus.sh && ./dokus.sh
            ```

            **Windows:**
            1. Download `dokus-quick-deploy.zip` from assets
            2. Extract and run `dokus.bat`

            **What's included:**
            - ✅ All 6 backend services (auth, cashflow, payment, reporting, audit, banking)
            - ✅ Interactive setup with auto-generated passwords
            - ✅ Multi-architecture support (AMD64 + ARM64)
            - ✅ PostgreSQL, Redis, RabbitMQ

            ## 📱 Mobile & Desktop Installation

            ### Android
            1. Download the APK from assets
            2. Enable "Install from unknown sources" on your Android device
            3. Install the APK

            ### macOS
            1. Download the DMG from assets
            2. Open the DMG file
            3. Drag the app to your Applications folder

            ## 🔐 Verification

            This release was automatically built and signed by GitHub Actions.

            - Workflow: [${{ github.workflow }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            - Commit: [${{ steps.version.outputs.short-sha }}](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
          files: |
            ./artifacts/*.apk
            ./artifacts/*.dmg
            ./artifacts/*.zip
          draft: false
          prerelease: true
          generate_release_notes: true

      - name: 🎉 Release created
        run: |
          echo "✅ Release ${{ steps.version.outputs.version-tag }} created successfully!"
          echo "🔗 https://github.com/${{ github.repository }}/releases/tag/${{ steps.version.outputs.version-tag }}"
