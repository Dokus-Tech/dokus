name: Test Suite

on:
  workflow_call:
    inputs:
      run-android:
        description: 'Run Android tests'
        required: false
        type: boolean
        default: true
      run-desktop:
        description: 'Run Desktop/JVM tests'
        required: false
        type: boolean
        default: true
      run-ios:
        description: 'Run iOS tests (expensive)'
        required: false
        type: boolean
        default: false

jobs:
  # Android unit tests (fast, runs on Ubuntu)
  android-tests:
    name: 🤖 Android Tests
    if: inputs.run-android
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup environment
        uses: ./.github/actions/job-set-up

      - name: 🧪 Run Android unit tests
        run: |
          echo "Running Android unit tests..."
          ./gradlew :composeApp:testDebugUnitTest --no-daemon --stacktrace

      - name: 📊 Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            **/build/test-results/**/TEST-*.xml
          check_name: Android Test Results
          comment_mode: off

      - name: 📤 Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-test-reports
          path: |
            **/build/test-results/**/TEST-*.xml
            **/build/reports/tests/**
          retention-days: 7

      - name: ✅ Tests completed
        if: success()
        run: echo "✅ Android tests passed!"

  # Desktop/JVM tests (fast, runs on Ubuntu)
  desktop-tests:
    name: 🖥️ Desktop Tests
    if: inputs.run-desktop
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup environment
        uses: ./.github/actions/job-set-up

      - name: 🧪 Run Desktop/JVM tests
        run: |
          echo "Running Desktop/JVM tests..."
          ./gradlew :composeApp:desktopTest --no-daemon --stacktrace

      - name: 📊 Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            **/build/test-results/**/TEST-*.xml
          check_name: Desktop Test Results
          comment_mode: off

      - name: 📤 Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-test-reports
          path: |
            **/build/test-results/**/TEST-*.xml
            **/build/reports/tests/**
          retention-days: 7

      - name: ✅ Tests completed
        if: success()
        run: echo "✅ Desktop tests passed!"

  # iOS tests (expensive, runs on macOS only when needed)
  ios-tests:
    name: 🍎 iOS Tests
    if: inputs.run-ios
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup environment
        uses: ./.github/actions/job-set-up

      - name: 🍎 Select iOS Simulator architecture
        id: arch
        run: |
          ARCH=$(uname -m)
          if [ "$ARCH" == "arm64" ]; then
            echo "target=iosSimulatorArm64Test" >> $GITHUB_OUTPUT
            echo "Running on Apple Silicon (ARM64)"
          else
            echo "target=iosX64Test" >> $GITHUB_OUTPUT
            echo "Running on Intel (x64)"
          fi

      - name: 🧪 Run iOS Simulator tests
        run: |
          echo "Running iOS tests for ${{ steps.arch.outputs.target }}..."
          ./gradlew :composeApp:${{ steps.arch.outputs.target }} --no-daemon --stacktrace

      - name: 📊 Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            **/build/test-results/**/TEST-*.xml
          check_name: iOS Test Results
          comment_mode: off

      - name: 📤 Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-test-reports
          path: |
            **/build/test-results/**/TEST-*.xml
            **/build/reports/tests/**
          retention-days: 7

      - name: ✅ Tests completed
        if: success()
        run: echo "✅ iOS tests passed!"

  # Run all common tests (shared code)
  common-tests:
    name: 🔄 Common Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup environment
        uses: ./.github/actions/job-set-up

      - name: 🧪 Run all common tests
        run: |
          echo "Running all platform tests..."
          ./gradlew allTests --no-daemon --stacktrace
        continue-on-error: true

      - name: 📊 Publish aggregated test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            **/build/test-results/**/TEST-*.xml
          check_name: Common Test Results
          comment_mode: off

      - name: 📤 Upload aggregated test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: common-test-reports
          path: |
            **/build/test-results/**
            **/build/reports/tests/**
          retention-days: 7

      - name: ✅ Tests completed
        if: success()
        run: echo "✅ Common tests passed!"

  # Test summary
  test-summary:
    name: 📊 Test Summary
    runs-on: ubuntu-latest
    needs: [android-tests, desktop-tests, ios-tests, common-tests]
    if: always()

    steps:
      - name: 📊 Generate test summary
        run: |
          echo "# 🧪 Test Suite Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Platform | Status | Duration |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------|----------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🤖 Android | ${{ needs.android-tests.result == 'success' && '✅ Passed' || needs.android-tests.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} | - |" >> $GITHUB_STEP_SUMMARY
          echo "| 🖥️ Desktop | ${{ needs.desktop-tests.result == 'success' && '✅ Passed' || needs.desktop-tests.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} | - |" >> $GITHUB_STEP_SUMMARY
          echo "| 🍎 iOS | ${{ needs.ios-tests.result == 'success' && '✅ Passed' || needs.ios-tests.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} | - |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔄 Common | ${{ needs.common-tests.result == 'success' && '✅ Passed' || needs.common-tests.result == 'skipped' && '⏭️ Skipped' || '❌ Failed' }} | - |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Overall status
          if [ "${{ needs.android-tests.result }}" == "failure" ] || \
             [ "${{ needs.desktop-tests.result }}" == "failure" ] || \
             [ "${{ needs.ios-tests.result }}" == "failure" ]; then
            echo "## ❌ Test Suite Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "One or more test suites failed. Please review the logs above." >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "## ✅ All Tests Passed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "All executed test suites completed successfully!" >> $GITHUB_STEP_SUMMARY
          fi
