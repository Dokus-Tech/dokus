# Dokus Deployment Environment Configuration
# Copy this file to .env: cp .env.example .env
#
# Goal: keep only critical secrets in env for the MVP.
# Non-secret defaults live in the service `application-*.conf` files.

# ============================================================================
# DEPLOYMENT MODE
# ============================================================================
# Determines how features like Peppol are managed:
# - cloud: Dokus manages credentials on behalf of users (requires master API keys)
# - self-hosted: Users configure their own credentials (default)
DOKUS_HOSTING_MODE=self-hosted

# ============================================================================
# REQUIRED - Secrets (auto-generated by ./dokus.sh setup)
# ============================================================================
DB_PASSWORD=
REDIS_PASSWORD=
MINIO_PASSWORD=
JWT_SECRET=

# ============================================================================
# OPTIONAL - Storage public URL (for presigned URLs accessible from clients)
# ============================================================================
# Cloud: https://your-domain.com/storage
# Self-hosted: http://<your-server-ip>:8000/storage
# STORAGE_PUBLIC_URL=

# ============================================================================
# OPTIONAL - External API Keys
# ============================================================================
# CBE API for Belgian company lookups (https://cbeapi.be)
# Default is already set in application.conf - only override if needed
# CBE_API_SECRET=

# ============================================================================
# OPTIONAL - AI Configuration
# ============================================================================
# AI Mode controls all model selection automatically:
# - light: qwen3:4b for all tasks (Raspberry Pi, low resources)
# - normal: qwen3:4b for fast tasks, qwen3:30b-a3b for extraction (self-hosted)
# - cloud: same as normal + provenance tracking via Claude API (Dokus-managed)
# Default is set per docker-compose profile.
# DOKUS_AI_MODE=normal

# Ollama host URL (defaults differ by profile)
# OLLAMA_HOST=http://localhost:11434

# Anthropic API key (optional, enables provenance tracking in cloud mode)
# ANTHROPIC_API_KEY=

# ============================================================================
# OPTIONAL - Cloud profile (HTTPS)
# ============================================================================
# DOMAIN=app.dokus.tech
# ACME_EMAIL=contact@dokus.tech

# ============================================================================
# CLOUD ONLY - Peppol Master Credentials
# ============================================================================
# Required when DOKUS_HOSTING_MODE=cloud
# These are Dokus's own Recommand API credentials, used for all cloud tenants.
# Users won't need to configure their own Peppol credentials.
# PEPPOL_MASTER_API_KEY=
# PEPPOL_MASTER_API_SECRET=
