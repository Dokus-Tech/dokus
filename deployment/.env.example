# Dokus Self-Hosting Environment Configuration
# Copy this file to .env: cp .env.example .env
#
# Most values are auto-generated by ./dokus.sh setup
# Only customize if you have specific requirements

# ============================================================================
# REQUIRED - Passwords (auto-generated by dokus.sh, change if needed)
# ============================================================================
DB_PASSWORD=
REDIS_PASSWORD=
RABBITMQ_PASSWORD=
MINIO_PASSWORD=
JWT_SECRET=

# ============================================================================
# OPTIONAL - Customization
# ============================================================================

# AI Configuration (Ollama runs on your host machine)
# Install Ollama: https://ollama.com/download
# Then run: ollama pull mistral:7b
AI_OLLAMA_URL=http://host.docker.internal:11434
AI_OLLAMA_MODEL=mistral:7b

# Lite profile (Raspberry Pi): use smaller model
# AI_OLLAMA_MODEL=qwen2:1.5b

# Pro profile (Mac/Server): use larger model
# AI_OLLAMA_MODEL=qwen2.5:14b-instruct

# OpenAI (alternative to Ollama)
# AI_OPENAI_ENABLED=true
# AI_OPENAI_API_KEY=sk-...
# AI_OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# ADVANCED - Usually no need to change
# ============================================================================

# Logging
# LOG_LEVEL=INFO

# Cache type: redis (default for pro) or memory (default for lite)
# CACHE_TYPE=redis

# CORS (default allows all origins)
# CORS_ALLOWED_HOSTS=*

# Rate limiting
# RATE_LIMIT_PER_MINUTE=60
