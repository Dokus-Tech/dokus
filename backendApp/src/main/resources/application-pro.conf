include "application.conf"

ktor {
    deployment {
        environment = "pro"
    }
}

# Production-ish defaults (self-hosted)
jwt {
    # In production, strongly prefer setting JWT_SECRET via env.
    secret = ${JWT_SECRET}
}
